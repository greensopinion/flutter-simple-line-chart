import 'package:flutter/material.dart';

class LineChart extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      color: Color.fromARGB(0xff, 0, 0, 0xff),
    );
  }
}
